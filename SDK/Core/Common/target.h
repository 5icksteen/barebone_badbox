#pragma once

#include "preprocess.h"

#include GEN_STM32_HEADER(TARGET_SERIES,)
#include GEN_STM32_HEADER(TARGET_SERIES, _gpio)
#include GEN_STM32_HEADER(TARGET_SERIES, _adc)
#include GEN_STM32_HEADER(TARGET_SERIES, _bus)